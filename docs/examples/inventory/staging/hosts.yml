---
# Staging environment inventory
# Production-like HA setup

all:
  children:
    proxmox_nodes:
      hosts:
        pm02-staging:
          ansible_host: pm02-staging
          ansible_user: root