---
# Production environment inventory
# Full HA setup with multiple workers

all:
  children:
    proxmox_nodes:
      hosts:
        pm02-prod:
          ansible_host: pm02-prod
          ansible_user: root