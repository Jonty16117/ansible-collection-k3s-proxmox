---
# Development environment inventory
# Minimal cluster for testing and development

all:
  children:
    # Proxmox host for VM management
    proxmox_nodes:
      hosts:
        pm02-dev:
          ansible_host: pm02-dev
          ansible_user: root

    # k3s_cluster and its subgroups are populated dynamically
    # by the cluster_config role - do not define them here