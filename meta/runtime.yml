---
# Runtime metadata for community.k3s_proxmox collection
# Defines minimum Ansible version and plugin routing

requires_ansible: ">=2.15.0"

action_groups:
  k3s_proxmox:
    - community.proxmox.proxmox_kvm
    - community.general
    - ansible.utils.ipaddr
    - ansible.builtin.add_host

plugin_routing:
  modules: {}
  action: {}